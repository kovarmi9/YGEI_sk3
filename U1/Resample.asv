clc; clear; format long G

MyImg=randi(255,16,16,3);



k=4;

[r,s]=size(MyImg);

nS=r/k;
nR=r/k;

NewImg=zeros(k);
for i=0:k-1
    for j=0:k-1
        NewImg(i+1,j+1)=MyImg(i*nS+1,j*nR+1);
    end
end